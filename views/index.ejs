<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task App</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>


</head>

<body>
    <div class="heading text-blue-600 text-center text-2xl bg-zinc-900">Add Your Task</div>
    <div class="main w-full h-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <div class="form">
                <form action="/create" method="post">
                    <input class="block  px-5 py-2 bg-zinc-300 mt-6 mb-6 ml-6 mr-6 border-solid" type="text" name="Title"
                        placeholder="Enter Title">
                    <textarea
                        class="block px-5 w-full rounded-mb  py-2 bg-zinc-300 mt-6 mb-6 mr-6 ml-6 border-solid resize-none"
                     name="details"   placeholder="Text Area"></textarea>
                    <input class="block px-5 py-2 bg-blue-600 mt-6 mb-6 mr-6 ml-6 border-double cursor-pointer"
                        type="submit" value="Submit">
                        &nbsp;
                </form>
            </div>
        </div>


        <div class="tasks p-10 text-white flex gap-3 flex-wrap">
            <% if(files.length> 0) { %>
                <% files.forEach(function(val) { %>
                    <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md">
                        <h1 class="text-white text-3xl tracking-tighter"><%= val.substr(0,val.length-4) %></h1>
                        <div class="flex w-full justify-between item-center mt-2">
                            <a class="text-blue-600 inline-block  "href="/files/<%= val%>">Read More.</a>
                            <a class="text-blue-600 inline-block "href="/edit/<%= val%>">Edit Filename</a>
                        </div>
                        
                    </div>
                    <% }); %>
                        <% } 
                        else { %>
                            <h3 class="text-blue-200 text-xl">No Tasks Yet</h3>
                            <% } %>
        </div>



    </div>





    </div>






    <script src="/javascript/script.js"></script>
</body>

</html>